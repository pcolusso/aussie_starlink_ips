require 'rake'
require 'rake/testtask'

# Define the test task
Rake::TestTask.new('test') do |t|
  t.pattern = 'test/**/*_test.rb' # Adjust this if your test files are located elsewhere
  t.verbose = true               # Enable verbose output
end
